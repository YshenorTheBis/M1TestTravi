language: python

sudo : false
python:
  - "3.6.4"
  
global:
  -act = logistic
  
matrix:
  - hid = 5
  - hid = 10
  - hid = 15
  - hid = 20
  - hid = 25
  - hid = 30
  
  
branches:
  only:
    - master
    
install:
  - pip install -q Django==2.0.3

script: python3 TP5.py $hid $act

